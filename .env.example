TYPESAFE_API_KEY=your_key_here
# Optional: omit for per-instance memory caching (also works on Vercel).
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
# Optional: use different namespaces for preview and production.
CACHE_NAMESPACE=probably:production:v1
